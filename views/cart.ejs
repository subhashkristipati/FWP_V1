<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Cart</title>
</head>
<body>
    <h1>My Cart: </h1>
    <% if(data.length>0){ %>
        <% data.forEach(item=>{ %>
            <img src="<%= item.img %>">
            <h3><%= item.name %></h3><span><%= item.price %> Rs</span><br>
            <h5><%= item.quantity %> Kg</h5><br><br>
        <% }) %>
    <% } %>
</body>
</html>